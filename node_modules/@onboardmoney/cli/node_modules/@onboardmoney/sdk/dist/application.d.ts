import type { TransactionReceipt } from '@ethersproject/providers';
import type { TxBatchDto } from './types/batch.interface';
export type { TransactionReceipt } from '@ethersproject/providers';
export type { TxBatchDto, TxRequestDto } from './types/batch.interface';
export declare class App {
    private apiKey;
    private baseURL;
    private axiosInstance;
    constructor(apiKey: string, baseURL: string);
    balance: () => Promise<{
        balance: string;
        relayAddress: string;
    }>;
    deploy: () => Promise<{
        appAddress: string;
    }>;
    createUser: () => Promise<{
        userAddress: string;
    }>;
    evaluateBatch: (batch: TxBatchDto) => Promise<{
        success: boolean;
    }>;
    sendBatch: (batch: TxBatchDto) => Promise<TransactionReceipt>;
    getPolicy: () => Promise<{
        policy: string;
    }>;
    updatePolicy: (policy: string, signerAddress?: string | undefined) => Promise<void>;
}
